server:
  port: 8002
spring:
  application:
    name: "gengine-ratelimiter"
  devtools:
    add-properties: false

  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      fail-fast: true